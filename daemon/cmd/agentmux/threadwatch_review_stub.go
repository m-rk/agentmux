package main

import "fmt"

// runThreadwatchReview is a placeholder for `agentmux threadwatch review`,
// implemented by a concurrently-developed change in
// daemon/internal/threadwatch/review*.go and
// cmd/agentmux/threadwatch_review.go. This stub exists only so this worktree
// builds on its own; the orchestrator deletes this file when merging that
// change in, at which point the real runThreadwatchReview (same signature)
// takes over.
func runThreadwatchReview(args []string) {
	fmt.Println("not implemented")
}
